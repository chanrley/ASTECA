# Copie este arquivo para .env (não versionado) e defina um segredo forte antes de rodar `docker compose up`.
JWT_SECRET=troque-este-valor-para-uma-string-aleatoria-longa

# Porta exposta pelo container do frontend (Nginx) no host. Opcional, padrão 8081.
FRONTEND_PORT=8081
